---
import Layout from '../../layouts/Layout.astro';
import SlidePlayer from '../../components/SlidePlayer.astro';
import { getEntry } from 'astro:content';
const base = import.meta.env.BASE_URL;

const entry = await getEntry('formation', 'bloc-05');
if (!entry) return Astro.redirect(`${base}formation/`);

const nextBlocUrl = `${base}formation/`;
---
<Layout title={entry.data.titre}>
  <SlidePlayer {entry} blocId="bloc-05" {nextBlocUrl} phaseLabel="Ouverture" />
</Layout>
